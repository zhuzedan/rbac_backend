<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zzd.mapper.SystemMenuMapper">

    <select id="findSystemMenuListByUserId" resultType="string">
        SELECT DISTINCT perms from t_system_menu where id in (
            SELECT menu_id  from t_system_role_menu where role_id in (
                SELECT role_id from t_system_user_role  where user_id=#{userId}
            )
        ) and status='1'
    </select>
</mapper>